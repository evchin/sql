#ifndef TYPEDEFS_H
#define TYPEDEFS_H

#include "../bplustree/multimap.h"
#include <vector>
using namespace std;

typedef vector<string> vectorstr;
typedef MMap<string, string> mmap_ss;

#endif // TYPEDEFS_H